name: Update CDA stylesheets
permissions:
  contents: write
  pull-requests: write
on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:
jobs:
  update-stylesheets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - run: pip install -e .
      - run: PYTHONPATH=.. python .github/scripts/update_stylesheets.py
      - name: Detect stylesheet changes
        id: detect_stylesheet_changes
        run: |
          git add -A
          if git diff --cached --quiet; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          fi
      - if: steps.detect_stylesheet_changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: 'chore: Update CDA stylesheets'
          title: 'chore: Update CDA stylesheets'
          body: |
            Updates CDA stylesheets from the HL7 repository:
            - Latest version from https://github.com/HL7/cda-core-xsl
            - Adds proper attribution headers
          branch: update-cda-stylesheets
          delete-branch: true
